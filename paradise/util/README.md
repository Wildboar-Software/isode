# paradise/util — `make-lib.sh` overlay

PARADISE replacement for `util/make-lib.sh` (how `libisode` / `libdsap` archives
are built). The live script is `util/make-lib.sh` at the repo root. This copy is
kept so the overlay can be compared with ISODE 8.0.
