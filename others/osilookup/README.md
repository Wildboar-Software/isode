# others/osilookup — SunLink OSI hosts → `isoentities`

Converts SunLink `/etc/sunlink/osi/hosts` (or older `/usr/etc/osi.hosts`)
entries into ISODE `isoentities` network-address syntax. Required when using
SunNet OSI 5.2+ so the local NSAP is registered. See `READ-ME` and
`doc/isode-gen.8.md`.
