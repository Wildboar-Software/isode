# others/rfa — Remote File Access

Contributed by Oliver Wenzel (GMD Berlin, 1990). Keeps a partial filesystem tree in sync between two sites as master vs slave files (not full FTAM).

Compile-time options are in `config.h`; runtime setup is `rfatailor`. `rfa.tr` / `rfa.ps` are the design notes. See `READ-ME`.
