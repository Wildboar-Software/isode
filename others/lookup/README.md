# others/lookup — Password-lookup ROSY demo

The Volume 4 cookbook example: a ROS specification that looks up UNIX password-file fields over remote operations. Read this together with `imisc/` if you want the smallest path from `.ry` file to running initiator/responder.

See `READ-ME`. Not a security product; it is a teaching program.
