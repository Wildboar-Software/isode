# others/quipu/uips/manage — `dish_manage`

DISH variant focused on alias management (`add_alias`, `del_alias`,
`alias_chk`). Links `quipu/dish/libdish.a` plus `libmanage.a`. Installed as
`dish_manage`. Optional GNU Readline, same as DISH.
