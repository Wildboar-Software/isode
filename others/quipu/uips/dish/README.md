# others/quipu/uips/dish — DISH extras

Add-ons for the DISH built in `quipu/dish/`: `new_quipurc` can create a user's `.quipurc` when `quipurc on` is set in `dsaptailor`. Edit `dishinit` with the manager DN/password and local subtree **before** `make inst-all`. See `READ-ME`.
