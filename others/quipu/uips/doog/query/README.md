# others/quipu/uips/doog/query — Doog query engine

Asynchronous UFN query implementation used by `doog`. This is the interesting `libdsap` async example; the parent `doog/` directory is the interactive shell around it. See the `README` in this directory for build notes specific to the query library.
