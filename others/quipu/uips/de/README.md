# others/quipu/uips/de — Directory Enquiries

Curses public-access DUA from COSINE/PARADISE. Same program as `paradise/others/quipu/uips/de/`. Configure `detailor` (`dsa_address`, `username`, `default_country`, `default_org`). `./make termhelp` then `./make inst-all`. Run `de`.

Documented in `doc/isode-8-update.pdf` Appendix A. This is the DUA the top-level `paradise-dua` target builds from the main tree (`PARADISEDUA = others/quipu/uips/de`).
