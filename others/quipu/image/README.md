# others/quipu/image — Faces from the Directory

Programs that fetch photo attributes and show “who is logged in” style faces
(`xwho`, `xface`). Depends on `photo/` being installed. Historic integration
with MH 6.5 for mail composition is described in `READ-ME`. Bitmap conversion:
`pr2pe` (Sun raster → ASN.1 BIT STRING).
