# others/quipu/quipu-db/organisation — First organisational DSA

Example EDB for the first Directory System Agent of an organisation (holds the
organisation entry and typically slaves of superior context). Copy and rename to
match `mydsaname` / the DIT you actually run. See the `READ-ME` here and the
parent [quipu-db README](../README.md).
