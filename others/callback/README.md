# others/callback — Dual-session callback demo

Demonstration of listening and of two session connections in flight (ISODE
“callback”). See `READ-ME`. Useful when bringing up a new network backend, not
something to install in production.
