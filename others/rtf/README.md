# others/rtf — Reliable-transfer file copy

Small file-transfer utility (`rtf` / `rtfd`) used to test RTS upcalls. ASN.1 is `rtf.py` (PEPY input, not Python). Links `-lcrypt` for simple authentication. See the `rtsap/` README for the service this sits on.
